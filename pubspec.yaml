name: state_management_clean_arch
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_clean_architecture: ^5.0.0
  page_transition: ^2.0.2
  day_night_time_picker: ^1.0.2
  firebase_core: ^1.6.0
  cloud_firestore: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
